services:
  - type: web
    name: contaemdia-api
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt && mkdir -p data
    startCommand: python web_app.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
      - key: SECRET_KEY
        value: "sua_chave_secreta_super_segura_aqui_12345"
